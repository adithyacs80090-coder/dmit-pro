[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm run build"

[deploy]
startCommand = "pnpm --filter @dmit/api start"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
